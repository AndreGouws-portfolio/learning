# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

This repository currently contains no source code — only `README.md` (which reads "learning / my first repo") and git history. There is no build system, package manifest, test suite, linter, or application code to run yet.

There are no build, lint, or test commands to document because none exist in the repo. There is no architecture to describe because no code has been written.

When code is added to this repository, update this file to reflect:
- The language/framework chosen and how to install dependencies
- Commands to build, lint, run, and test the project (including running a single test)
- The high-level architecture once it spans more than one file
